export type Product = {
  id: number;
  title: string;
  price: number;
  thumbnail: string;
}
const data: Product[] = [];

export default data;

export type Message = {
  emailUser: string;
  message: string;
}